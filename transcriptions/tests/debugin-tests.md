Let's see what to do if a test fails and how to handle it.Let me just break this test.So I'll just add here an invalid value like break this.Okay.And now this test should fail.So if I say DBT test then what?I should see that out of our four tests, one is failing.So it's a pass pass pass fail.Amazing okay, so we had all of our tests executed and it says that it it gives this this a crazy longname.It's an internal name that the accepted value is the classroom type entire home blah blah blah.You see, the test with this crazy long name has failed.Now here is another trick.Before we go into what to do about it, you can pass dash X at the end of the test and then it willstop the test execution after the first failure so it fails earlier.Okay, so we already have a passing test because this test wasn't the first test to execute.But you see that, uh, it just canceled, for example, this specific test because we had a failedtest.Okay.So that speeds up development a little bit.And what I want to show you is that you can always take a look at the compiled test code.So here in the target folder you have a target folder here right.Uh if in VS code you just control click it or command click it.Then you will see the actual SQL for the test.So if remember a test shouldn't return any value right?At the end of the day this is what what's behind the scenes.So it says that we are grouping by the room type.So we are selecting every room type in this test.This is what DBT generated.And we are interested in those values which are not part of this list.This is the DBT transformed our generic test into an executable SQL actually.So let's copy this and paste this into a snowflake SQL worksheet.I'm going to copy this here and execute it.And you will see right away that here is the value.That's not part of our list.All right.