Okay, now let's make this project foolproof.First, we're gonna implement generic tests, and I will show you all four kinds of generic tests thatcome prepackaged with DBT.Our table that we're gonna use is the dim listings cleanse table.And if you take a look just as a reminder, what do we have here?We have a listings ID and the listings ID is numeric.And it shouldn't be null.Right.And it should also be unique because this is basically our primary key.So this listings ID we have a listing name.The listing name should definitely not be null but it doesn't need to be unique.And then we have well a few more.Let's say we let's implement the test for for room type.Now the room type.If you take a look you can simply execute a select this thing on it if you want.It only comprises of a few values.So entire home, apartment or private room.And there are.I believe uh, two more that you will find if you do select this thing.And then we have also host ID, which is again not null.Right.Uh, it doesn't need to be numeric because one host can have multiple listings, but every host ID heremust be a valid ID in our host table.So let's go on and implement the test for this.We're going to create a new file here, which will be a central file for every project that you workwith.And it's called schema YAML in the projects folder.So that's a YAML file.Uh, besides sources dot YAML.Now a quick note here.Schema is only convention.You will find it in virtually every project.But you can call this file whatever that YAML.Generally speaking in DBT, you can put as many YAML files in the models folder or in subfolders asyou wish with the names that you wish.And as long as every property for a certain model or source is listed in a single YAML file, you aregood to go.All right.So again schema.It's a convention that we have our main logic here.Now another note if you ever work with DBT projects, especially older DBT projects, you will manytimes see version two line at the top of YAML files like in the DBT projects or any other YAML.So this is there for historical purposes and and it is now completely optional.It just meant earlier that this is a certain version of YAML descriptor syntax in DBT.So I will just go and get rid of this test.Can test model sources or any other entity.Now we work with models.So you would say models and then add My model, which will be the dim listings cleansed.And we want to test columns.Namely, let's start with the listing ID column and then I can implement my data test.I want to ensure that the values are unique and not null.So these two are built in data tests.You can kick off the execution of a test suite by typing and executing DBT test, and you will see thatDBT will find these two tests and their pass.Amazing.So here you can see that we have two data tests and both of them are passing.Wonderful.Now here is one other thing I want you to remember.And this is this data test tag.Historically this was called test.So no data tests and it's still valid.You might have a deprecation warning eventually if you leave it at test.And the new Notation is data test.But remember, if you ever see tests instead of data tests, it might still work.Actually, even some of the recordings in this course you might see tests here instead of data tests,for example when we work with documentation.So please don't worry about those.Those are just videos from that time when the unit tests and the data tests weren't separated so explicitly.Let's implement a bunch of other tests, and let me just paste this here from the course resources.So I'm adding a few more tests for host ID if you remember, we discussed that it shouldn't be nulland that it is basically a foreign key to the dim host cleansed tables.Host ID column.So I'm adding a relationships test.And then for the room type, if you remember we discussed that the accepted values can be these fourvalues.So I'm just adding these here too.Now let's execute the test suite again.Okay.All of our tests are passing.Amazing.So please remember that there are four built in tests in DBT.Unique, not null relationships and accepted values.You might ask why we need unique and not null tests when we can enforce this on the data warehouse layer.If I go to Snowflake's constraints documentation, then you will see the snowflake supports unique andforeign keys and also not null columns.So you can enforce these on the data warehouse level in snowflake.But if you use other modern data warehouse providers like Databricks or Athena or Redshift, some ofthese constraints might be missing on the data warehouse level.So that's why it makes sense to explicitly test them out in DBT.So far we got lucky because all of our tests have passed.And in the next video I will show you what to do if a test fails.