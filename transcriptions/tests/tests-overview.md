Data tests are probably the most sophisticated of the three.DBT has two types of datasets singular and generic.Generic tests are common tests that are available plug and play in DBT, and there are four types ofbuilt in generic tests.First, you can ensure that the column only has unique values.Then you can also ensure that the column has no null values.You can also define a set of accepted values.A column must then comprise only of the values in this list, and you can also define relationships,which is a little bit like foreign keys in a database.Now if that wouldn't be enough for a production use case, DBT provides you with the possibility towrite singular tests, which you can then implement as SQL queries.The evaluation logic is very simple.When DBT executes the query that you wrote and they return any record, then your test will fail.If you see this in practice in the next videos, we'll also see later in the course that you can defineyour own custom generic tests too, so you can extend generic tests with your own implementation, andyou'll see that you can also import tests from third party DBT packages.Now let's go and get our hands dirty.